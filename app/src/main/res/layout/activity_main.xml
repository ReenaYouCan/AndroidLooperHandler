<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="15dp"
        tools:context=".ui.MainActivity">

        <Button
            android:id="@+id/btnTaskOne"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:onClick="onStartOrQuitTaskButtonClick"
            android:text="Run Task One"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/btnTaskTwo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:onClick="onStartOrQuitTaskButtonClick"
            android:text="Run Task Two"
            app:layout_constraintTop_toBottomOf="@+id/btnTaskOne" />

        <Button
            android:id="@+id/btnQuitLooper"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:onClick="onStartOrQuitTaskButtonClick"
            android:text="Quit Child Thread Looper!"
            app:layout_constraintTop_toBottomOf="@+id/btnTaskTwo" />


        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@+id/btnQuitLooper"
            tools:text="Message" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>